services:
  db:
    image: postgres:16-alpine
    container_name: fms-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER:-fms_admin}
      POSTGRES_PASSWORD: ${DB_PASSWORD:?DB_PASSWORD is required in .env.production}
      POSTGRES_DB: ${DB_NAME:-fms_production}
    volumes:
      - fms_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-fms_admin} -d ${DB_NAME:-fms_production}"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - fms-net

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fms-app
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "${APP_PORT:-3010}:3010"
    environment:
      NODE_ENV: production
      PORT: 3010
      HOSTNAME: "0.0.0.0"
      DATABASE_URL: postgresql://${DB_USER:-fms_admin}:${DB_PASSWORD:?DB_PASSWORD is required in .env.production}@db:5432/${DB_NAME:-fms_production}
      AUTH_SECRET: ${AUTH_SECRET:?AUTH_SECRET is required in .env.production}
      AUTH_TRUST_HOST: "true"
      APP_URL: ${APP_URL:-http://localhost:3010}
      BOOTSTRAP_ADMIN_EMAIL: ${BOOTSTRAP_ADMIN_EMAIL:-admin@app.local}
      BOOTSTRAP_ADMIN_PASSWORD: ${BOOTSTRAP_ADMIN_PASSWORD:?BOOTSTRAP_ADMIN_PASSWORD is required in .env.production}
    volumes:
      - fms_uploads:/app/public/uploads
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--spider", "http://127.0.0.1:3010/"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 20s
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 2048M
    networks:
      - fms-net

volumes:
  fms_pgdata:
    name: fms_pgdata
  fms_uploads:
    name: fms_uploads

networks:
  fms-net:
    name: fms-net
    driver: bridge